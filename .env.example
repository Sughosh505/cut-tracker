VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# TEMPORARY: set to true to run the app without a Supabase project, using a
# fake signed-in user and localStorage instead of the real database. Leave
# false/unset once VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY are filled in.
VITE_DEV_MODE=false
